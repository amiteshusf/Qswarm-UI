# Copy to `.env.local` or `.env.production` as needed.

# QSwarm API origin (no trailing slash). When unset, requests use same-origin `/api/v1/...`.
# VITE_API_BASE_URL=https://your-qswarm-api.example.com

# Set to `true` to use the built-in mock dataset (no network).
# VITE_USE_MOCK_DATA=true
