# Local development: use embedded mock API (no backend required).
VITE_USE_MOCK_DATA=true

# When talking to a real QSwarm host, set the API origin (no trailing slash)
# and set VITE_USE_MOCK_DATA=false (or remove it).
# VITE_API_BASE_URL=https://qswarm-api.example.com
